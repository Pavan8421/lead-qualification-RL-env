# GRPO training stack — Apple Silicon (MPS) / CPU profile.
# Use this on Mac M-series or any machine without CUDA/bitsandbytes.
# Trade-off vs requirements-train.txt: no 4-bit, no Unsloth Triton kernels,
# slower throughput. Fine for iterating with Qwen2.5-0.5B-Instruct on MPS.
torch>=2.2          # MPS support is solid from 2.2+
transformers>=4.45
accelerate>=0.34
peft>=0.12
wandb>=0.18
pyyaml>=6.0
httpx>=0.27
pydantic>=2.7
